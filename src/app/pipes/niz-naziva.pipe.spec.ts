import { NizNazivaPipe } from './niz-naziva.pipe';

describe('NizNazivaPipe', () => {
  it('create an instance', () => {
    const pipe = new NizNazivaPipe();
    expect(pipe).toBeTruthy();
  });
});
