import { PrvaFotoNizaPipe } from './prva-foto-niza.pipe';

describe('PrvaFotoNizaPipe', () => {
  it('create an instance', () => {
    const pipe = new PrvaFotoNizaPipe();
    expect(pipe).toBeTruthy();
  });
});
