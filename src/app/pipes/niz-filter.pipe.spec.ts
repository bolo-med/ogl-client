import { NizFilterPipe } from './niz-filter.pipe';

describe('NizFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NizFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
