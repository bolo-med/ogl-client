import { ValidacijaKorisnickogDirective } from './validacija-korisnickog.directive';

describe('ValidacijaKorisnickogDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidacijaKorisnickogDirective();
    expect(directive).toBeTruthy();
  });
});
